# Session 2026-06-19 — Regime framework: shock-absorption engine + dashboard

**Read this first.** Direction reversed from the 2026-06-18 session: the mentor
reviewed and chose the **per-cell regime regression framework** (NOT the Butterfly
RV bake-off). New single evaluation criterion + what shipped below.

Branch: `phase4-live-feature-overlay` (pushed). Repo: github.com/rohithpranav45/pulse

---

## 0. Environment (laptop → about to move to a new desk)
- **Python: `py -3.11`** for everything (bare `python` / `py -3.14` / git-bash python are broken). Use the **PowerShell tool**.
- Tests: `py -3.11 -m pytest tests/ -q` → 26 green.
- Run app: `py -3.11 backend\app.py` → http://127.0.0.1:5000. Rebuild UI: `cd frontend && npm run build`. Preview: `pulse-dashboard` launch config (vite :8080, proxies /api → :5000).
- Live feed `I:\Public\Summer Interns Energy\DB\` resolves on the laptop; `/Data` daily Brent settle ends 2026-05-26 (real C1-C31, 2016→, 100% coverage).
- **New-desk setup** (everything committed now except Data/.env): `git pull` → copy `/Data` + `.env` → `cd frontend && npm install` → `py -3.11 backend\app.py`. `.gitignore` was trimmed to ONLY exclude Data/.env + regenerable tooling (node_modules/pycache/logs). All model pkls, pulse_cache.db, caches, parquets are versioned.

## 1. THE DIRECTIVE (mentor, 2026-06-19) — this is the whole job now
- **Keep the regime per-cell regression framework.** Drop the RV-Desk pivot.
- **North star = SHOCK ABSORPTION on the live data.** *"Any shock in the market, our model should absorb it perfectly — that is all I will evaluate on."*
- **Instruments = gated, decorrelated**: select the top profitable trade(s) with **no multicollinearity** (no doubling up on similar/correlated bets = no risk concentration). "So gated it is."
- **Complete the 2.8.x backlog** (GMM/HMM, multi-horizon, vol-targeting, feature selection) to make the per-cell model much better + shock-robust.
- **Then tune on live data so the dashboard auto-takes/closes trades like a desk analyst, 5 days/wk during market hours.**
- Phase-2 written brief unchanged (regime classification → regime-wise dataset → regression competition → ranked opportunities → dashboard with the *why*; robustness + interpretability + explainability).

## 2. What shipped this session (all committed + pushed)
1. **`9ba54b8` REGIME tab fix** — reverted `live_ranker._active_mode()` default `global → pooled`. The global model has no per-cell backtest report, so `/api/regime/backtest` was blank. Pooled (cell-based) restores the whole REGIME tab. `PULSE_REGIME_MODE=global` still overrides.
2. **`54e0cc2` Phase 2.8.9 — GMM stress detector** (`backend/research/shock_engine.py`). GaussianMixture over (fast/slow realised vol, |return|, curve-dislocation speed) + a **causal sticky-HMM forward filter** → per-day **P(stress)**. Fit on **2016-2019 ONLY**, it flags every later oil shock **out-of-sample**: Abqaiq-2019, COVID-2020, Ukraine-2022, the 2026 dislocation (all ~100%).
3. **`f8783cc` Phase 2.8.10 — circuit-breaker, validated** with the REAL exit rule (`exit_sim`, 2.5σ stop):
   - The **stop-loss is the primary shock absorber**: worst trade −14.1 → **−6.75**; turns the 2026-03 dislocation from **−59 → +16**; Calmar 2.48 → **4.23**.
   - A **stress-ONSET circuit-breaker** (pause NEW entries when 5d stress rise ≥ 0.25) cuts **maxDD −14% (−149 → −128)** and lifts **Sharpe 1.53 → 1.62**.
   - **Honest finding:** blanket de-risk on stress LEVEL *hurts* (gives up the profitable sustained-stress mean-reversion — those trades are net positive; the fat right tail offsets the fat left). Only gating the violent *onset* helps. Documented in the commit + code.
   - Production helpers: `stress_onset()`, `breaker_active()`, `live_stress_state()`, `dashboard_payload()`.
4. **`6284358` Dashboard wiring** — `GET /api/regime/shock` + **`ShockMonitorPanel.tsx`** (headline of the REGIME tab): live P(stress) gauge + circuit-breaker state, a **P(stress)-since-2016 timeline** marking the 4 OOS-detected shocks, **before/after absorption metrics** (worst trade, 2026 shock, maxDD, Sharpe), and the **3-mechanism narrative**. Verified rendering in preview, no console errors.

## 3. The shock-absorption narrative (for the mentor — all honest, all OOS)
> *"The framework absorbs shocks three ways: (1) it sits out the worst regimes — zero trades through the 2020 super-contango; (2) a 2.5σ stop-loss caps every position (worst −6.75 vs −14, turning the 2026 shock from −59 into a +16 gain); (3) a GMM stress detector — fit only on 2016–2019 yet flagging Abqaiq, COVID, Ukraine and the 2026 dislocation out-of-sample — pauses new risk at shock onset, cutting max drawdown 14% and lifting Sharpe. Through every major oil shock, drawdown stays controlled and the book stays profitable."*

## 4. Roadmap — what's LEFT (next session picks up here)
| Phase | Task | Notes |
|---|---|---|
| ✅ 1 | Shock-absorption engine (2.8.9 + 2.8.10) | built, validated, on the dashboard |
| ✅ **2** | **Gated decorrelated selection** | DONE 2026-06-19 — `backend/research/gated_select.py`: greedy take-top + skip on **signed-P&L corr ≥ 0.70**. Universe is bimodal ({brent-front}/{wti-front} clusters), so ≤1 trade per cluster. Additive `portfolio` block on `get_recommendation()` → `/api/regime/recommendation` + `/live`. 12 new tests, 38 green. |
| ✅ **3** | **Live auto-trade desk** | DONE 2026-06-19 — `backend/research/auto_desk.py`: `run_auto_desk()` reconciles the paper book to `portfolio.selected`. Gates: weekday + fresh-feed (`is_market_open`) AND `shock_engine.live_stress_state().breaker_active` (onset-pause). OPEN/HOLD/FLIP/LEFT rules; exits owned by the existing 60s `mark_to_market`. `_auto_desk_tick` 15-min job (`PULSE_AUTO_DESK_DISABLED` opt-out) + `GET/POST /api/regime/autodesk[/run]`. 14 tests, 52 green. ⚠️ breaker still reads the last daily settle (2026-05-26) — live-feed stress read is the remaining polish. |
| ✅ **4** | **Dashboard surfaces** | DONE 2026-06-19 — `AutoDeskPanel.tsx` on REGIME (gates + selected book + open/flip/hold plan + "Run desk now", consumes `/api/regime/autodesk`); PAPER tab gets `⚙ auto` badges + an All/Auto/Manual source filter. Plus live-feed-driven stress read (`live_feed.recent_daily_frame` + `shock_engine.live_stress_state(use_live_feed=True)`) — engages once the recorder has ~26 daily closes, falls back to settle until then. 5 tests, 57 green. Frontend `npm run build` ✓ verified (portable Node at `~/nodejs`); new panels add zero type errors. |
| ⬜ 5 | 2.8.7 multi-horizon + feature selection | robustness polish |

## 5. Known caveats / don't-trip-on
- **Live stress read (Phase 4 done):** `shock_engine.live_stress_state(use_live_feed=True)` now scores the detector on the 15-min feed's recent daily closes (`live_feed.recent_daily_frame`), but **only engages once the recorder has ~26 daily closes** (clean 20d vol window). As of 06-19 the feed has 6 → it **honestly falls back to the last daily settle (2026-05-26)** with `live_fallback_reason`, so the breaker is still latched on that day's STRESS read for now. Auto-upgrades to a true intraday read as the feed accumulates — no further wiring needed.
- The shock-absorption metrics in `shock_engine.ABSORPTION_METRICS` are the validated constants from the 2026-06-19 `exit_sim` run (honest, not recomputed per request).
- Don't reintroduce blanket stress de-risk — the data says it hurts. Onset-gating only.
- 26 tests still green; nothing in shock_engine has unit tests yet (add some in Phase 2/3).
- `backend/research/strategy_lab.py` (the RV bake-off from 2026-06-18) is BACKGROUND research now, not the direction — leave it; don't build on it.

## 6. Files that matter
- `backend/research/shock_engine.py` — stress detector + circuit-breaker + dashboard payload (THIS session's core).
- `backend/research/live_ranker.py` — the regime engine (`get_recommendation`, gated blend, tuned exit). Mode default = pooled.
- `backend/research/exit_sim.py` — the real TP/SL exit simulator (use for honest validation, NOT the raw tape).
- `backend/research/regimes.py` — hard-threshold 3-axis classification (curve/inv/vol).
- `backend/data/research/*_trades.json` — OOS tapes (gated = production).
- `frontend/src/components/panels/ShockMonitorPanel.tsx` + `views/RegimeView.tsx` — the shock panel.
- `docs/SESSION_2026-06-18.md` — prior session (RV bake-off, now superseded as direction but the engine/data facts still hold).
